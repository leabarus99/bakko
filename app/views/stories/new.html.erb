 <%= simple_form_for([@trip, @story]) do |f| %>
    <%= f.input :title %>
    <%= f.input :trip %>
    <%= f.input :location %>
    <%= f.input :content%>
    <%= f.input :photo%>
    <%= f.input :photo, as: :file, label: "Add a picture" %>
    <%= f.input :video, as: :file, label: "Add a video"  %>
    <%= f.submit :class => "create-article" %>
<% end %>

  <%= link_to "Back journal", journal_path %>
